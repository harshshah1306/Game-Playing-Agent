#to reverse a string at index n and skip n indexex
#approch->divide the string in 'n' parts and reverse the even #ones
import sys
data = sys.stdin.read().splitlines()
d=list(data[0])
n=int(data[1])
datalist=[]
check=1
strin=''
ans=''
for i in range(0,len(d),n):
    strin=''
    for j in range(i,n+i):
        if(j!=len(d)):
            strin=strin+d[j]
        else:
            datalist.append(strin)
            check=0
            break
    if(check==1):
        datalist.append(strin)
rev=''
s=''
for i in range (len(datalist)):
        if(i%2==0):
            ss=datalist[i]
            rev=rev+ss[::-1]
        else:
            rev=rev+datalist[i]
print (rev)

        